/**
 * Created by quzhou on 10/24/17.
 * Find median from large file of integers
 *
 * 思路就是：先找在INT_MIN和INT_MAX的median（0？），
 * 然后读large file of integers，找出比这个数小的个数是否有一半，然后调整二分的边界
 */

import java.util.*;

public class MedianFromFile {
    double findNth(int N, int left, int right) {
        return 0.0;
    }
}
